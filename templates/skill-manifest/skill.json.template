{
  "schema_version": "1.0.0",
  "name": "example-skill",
  "version": "0.1.0",
  "description": "One-line summary of what this skill does.",
  "instructions": [
    "Describe required behavior, constraints, and safety boundaries."
  ],
  "scripts": {
    "validate": "scripts/validate.sh"
  },
  "hooks": [
    {
      "event": "pre-dispatch",
      "handler": "handlers.py:pre_dispatch",
      "timeout_seconds": 3.0,
      "idempotent": true
    }
  ]
}
